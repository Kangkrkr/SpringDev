server:
  port: 8088

spring:
  redis:
    host: localhost
    port: 6379
  profiles:
    active: local

kafka:
  bootstrapAddress: localhost:9092

message:
  topic:
    name: mytopic
greeting:
  topic:
    name: greeting
filtered:
  topic:
    name: filtered
partitioned:
  topic:
    name: partitioned